---
name: example-echo
description: Use the external example.echo tool to return a user-supplied message unchanged.
---

# 外置 Skill 示例

当用户明确要求测试外置 Skill 或 Echo 工具时，调用 `example_echo`，并把用户希望回显的文本放入 `message` 参数。直接展示工具返回的 `message`，不得添加或改写文本。

